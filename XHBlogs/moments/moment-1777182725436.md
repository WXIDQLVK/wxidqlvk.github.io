// 1. 获取当前实时时间（ISO 标准格式，或你想要的格式）
const nowIso = new Date().toISOString(); 
// 如果你需要普通格式（如 "2026-08-10 19:32:00"），可以用前面那段格式化代码

// 2. 自动生成唯一 ID（防止冲突）
const uniqueId = `moment-${Date.now()}`;

// 3. 自动组装要保存的 Markdown 内容
const fileContent = `---
id: "${uniqueId}"
date: "${nowIso}"
location: "北京市 东城区"
images:
  - '/moments/day1.webp'
---

今天又是充满希望的一天！`;

// 4. 后台代码通过 fs 模块自动写入文件保存（例如保存到 moments 文件夹下）
// fs.writeFileSync(`./moments/${uniqueId}.md`, fileContent);
